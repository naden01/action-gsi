name: ErfanGSI-build

on:
  workflow_dispatch:
    inputs:
      ROM_URL:
        description: 'Direct link/直链'
        required: true
        default: 'https://download.mifirmpremium.cloud/2b443cffea0c8ed7c2b1136bffaef047_bxh8LkC7QGhr3NNCXSNhBRpDX9WAt_TF3tDZPfhhlBHmAyt3Nx1zcva3nldo23VREPn_-oLYGyyguaB_y1baMLwCl40rf1aJdFUiY23tXiRAhKI5gI6YzsXQ9ATVbDQfBcdE4ZJJKGqRPXUeTKPw9c_R3v4Rau92KWSgV9IZ8kEPFL9t2owxs1S2IfrVKCr5d8gq5njMF_-MKvQ4QH1ON9IPEEy9PF7w6CgtTlk-VYsaP0IPQoY0H6x6QziiLb4xKUlwoowSXdjI195eNsavUvZmZq24NoHKXX7NdIQK-9vgxQ-NRSNuuV7bvhdQLrFSThahJUCiE_Hg6XltZ16An7dyaZNy77UnDzBir_L5AsQ9gTbDz-FzSti4Iq0yGQBugzXO15N-9_4rCaNfBAqMShke5BP4JlClx1Dp1whsjsjRoumknGVvMzpUebk7ZUqI7H5JGkzcaL_N0cxz24OGCW611I9fzHe8fYI0gq_Kum9Ljkg4z3QgnZu-xegm9h0th4cEPPzQzwGze6TDGUxg0lop4198fWbmkK-FKDElU1-ugKudtL0LBtDE9ydNMZijZAKaZwbIKcPy4Fsan0T3aVo7ALhT0tpoXNXvxzKAehWFQm_VzPWzSmkuVZ-419EZYdZTK5u7l24ZNeoEJp9Wxguyfu0UfBxcFcaAd05Y9JlKNPNeG1ZC5BxQe5B9Hc2TrE-SCIqFbyQlSzW2EwYHDu0sd-T0qr61QkQOHRv7_O1PtJ8kWAxWl_vvxdX8kyibbAt74I9xYN7hhvHEFOYMNnnh2aYUOa9x_y6tmeptLpno_FK2KzEaKOmek3yi9wPIJOCTIyzCu_2uH6sANP2vjP-JaonDl9p2xgn-8IFYALpXeRHKkgLXuCN0BRqFv_uCFjntD5OrffOuXYLXteBBY1DV0xMJaRIGQQOArvURPce0wcYAsGsmQ1EXOlMcagly4l1K8QjLkEZU_9rbE2Swh7PBq_sSco2BK1dFlD_onJJ7VMJhQPHbqhGV49OAvEgeDuttCYIapOJGl_ZlwKhT0dsEqaW6367EAgMFLzYT1uZ7UDPOG2kKianpKkY1kV4W2L4T78I8Ts8QlUiAbDdbg?file_name=redwood_global_images_V14.0.7.0.TMSMIXM_20231129.0000.00_13.0_global_d4c41f9bfc.tgz'
      REPACK_NAME:
        description: 'Compressed name/打包名称'
        required: true
        default: 'flawless.7z'
      ROM_TYPE:
        description: 'ROM type/ROM 类型'
        required: true
        default: 'Generic'
env:
  TELEGRAM_TO: 1214932938
  TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}

jobs:
  build_tosasitill_GSI:
    runs-on: ubuntu-20.04

    steps:
      - name: 获取源代码
        uses: actions/checkout@main
        with:
          path: ErfanGSIs

      - name: 获取变量
        run: |
          echo "BUILD_TIME=$(date +%s | md5sum | awk '{print substr($1,1,10)}')" >> $GITHUB_ENV
          echo "BUILD_sTIME=$(TZ=Asia/Shanghai date -d @$(date +%s) +'%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_ENV

      - name: 发送 Telegram 提醒消息 1/2
        uses: appleboy/telegram-action@master
        with:
         to: 1214932938
         token: ${{ secrets.TELEGRAM_TOKEN }}
         message: tosasitill GSI 开始构建！
                      构建开始时间：${{ env.BUILD_sTIME }}
                         提交链接：${{ github.event.inputs.ROM_URL }}
                             提交 GSI 类型：${{ github.event.inputs.ROM_TYPE }}

      - name: 配置环境‌‌
        run: |
          sudo apt install -y git wget git gzip unzip curl axel aria2 zstd rename p7zip-full pigz

      - name: 获取 ErfanGSI 工具
        uses: actions/checkout@main
        with:
          repository: tosasitill/erfanSGSI
          path: ErfanGSIs

      - name: 更换 777 权限
        run: |
          sudo chmod 777 -R $GITHUB_WORKSPACE/ErfanGSIs

      - name: 修复意外退出
        run: |
              sed -i 's/\|\| exit 1//g' ErfanGSIs/url2GSI.sh
              sed -i 's/exit 1//g' ErfanGSIs/url2GSI.sh

      - name: 建立并配置 ErfanGSI 工具所需环境
        run: |
          sudo sed -i 's/USE_MIRROR_FOR_PIP\=true/USE_MIRROR_FOR_PIP\=false/' $GITHUB_WORKSPACE/ErfanGSIs/setup.sh
          sudo bash $GITHUB_WORKSPACE/ErfanGSIs/setup.sh
          sudo apt-get -y --purge remove "adoptopenjdk-11-hotspot"
          java -version
          pip3 install aligo

      - name: 下载 ROM 并制作 GSI
        run: |
          cd ${{ github.workspace }}/ErfanGSIs/
          sudo bash ./url2GSI.sh --ab ${{ github.event.inputs.ROM_URL }} ${{ github.event.inputs.ROM_TYPE }}

      - name: 压缩 GSI
        if: ${{ github.event.inputs.ALI }} != '1'
        run: |
          cd $GITHUB_WORKSPACE/ErfanGSIs
          pwd
          7za a -mmt16 -t7z -r tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }} output/*
          ls "tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }}"
          mkdir upload_ab
          ls upload_ab
          if [ $(ls -l tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }} | awk '{print $5}') -gt 2147483647 ]; then tar cvzpf - tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }} | split -d -b 1024m - upload_ab/tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }}; else mv tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }} upload_ab/tosasitillGSI0202_${{ github.event.inputs.REPACK_NAME }}; fi

      - name: 发送 Telegram 提醒消息 2/2
        uses: appleboy/telegram-action@master
        with:
            to: 1214932938
            token: ${{ secrets.TELEGRAM_TOKEN }}
            message: tosasitill GSI 构建成功!
              构建 GSI 详细信息如下！
            document: ${{ github.workspace }}/ErfanGSIs/build_info.txt

      - name: 上传至 Github Releases
        if: ${{ github.event.inputs.ALI }} != '1'
        uses: ncipollo/release-action@v1.12.0
        with:
          artifacts: "${{ github.workspace }}/ErfanGSIs/upload_ab/*"
          tag: "${{ github.event.inputs.REPACK_NAME }}_${{ env.BUILD_TIME }}"
          bodyFile: "${{ github.workspace }}/ErfanGSIs/output/build_info.txt"
          token: ${{ secrets.GITHUB_TOKEN }}
